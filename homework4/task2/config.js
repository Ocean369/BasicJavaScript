'use strict';

/** Объект с настройками игры
 * @property {int} colsCount количество колонок в карте
 * @property {int} rowsCout количество строк в карте
 */
const config = {
    colsCount: 10,
    rowsCount: 10,
};